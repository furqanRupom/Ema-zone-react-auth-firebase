import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Login = () => {

    return (
        <div>
            login has done 
        </div>
    );
};

export default Login;